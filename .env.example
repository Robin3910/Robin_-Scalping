# ===== 必填 =====
# 面板登录密码（首次启动会写入 config/config.json）
ROBIN_PASSWORD=change-me-please

# ===== 真实交易模式（默认模拟盘，dashboard 切换）=====
BINANCE_API_KEY=
BINANCE_API_SECRET=

# 是否默认使用测试网（true=testnet，false=mainnet）。最终以面板「交易模式」开关为准
BINANCE_TESTNET=true

# Web 服务
WEB_HOST=0.0.0.0
WEB_PORT=8765
